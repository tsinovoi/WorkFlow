<% if current_user.try(:admin?) %>
  	<%= render 'pages/adminhome' %>
<% elsif user_signed_in? %>
	<%= render 'pages/userhome' %>
<% else %>
	<div class="hero-unit">
		<h1>Welcome to Work.Flow</h1>
			<p class="lead">
			  You have reached the home of <b>the</b> application for creating case mangement workflows.
			</p>
			<p>
			  <%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-primary btn-large" %>
			</p>
	</div>
			<div class="row-fluid">
				<div class="span9">
					<h1 class="text-center">What can it do?</h1>
				</div>
			</div>
			<div class="row-fluid">
				  <div class="span3">Collect Data</div>
				  <div class="span3">Assign Data to Users</div>
				  <div class="span3">Notify Users</div>
			</div>
<% end %>